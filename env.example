# Email Server Configuration
# Centralized Hackathon Demo Setup
# All users share the same email server (contact@amanulla.in)

# IMAP Configuration (Incoming emails)
IMAP_HOST=imap.hostinger.com
IMAP_PORT=993
IMAP_ENCRYPTION=SSL

# SMTP Configuration (Outgoing emails)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_ENCRYPTION=SSL

# Shared Email Account (Add the password for contact@amanulla.in)
EMAIL_USERNAME=contact@amanulla.in
EMAIL_PASSWORD=YOUR_PASSWORD_HERE

# Existing configurations
GEMINI_API_KEY=...
BUCKET=...
GOOGLE_CLOUD_PROJECT=...
CLOUD_RUN_URL=...
NODE_ENV="production"
GOOGLE_APPLICATION_CREDENTIALS=...
DB_USER=...
DB_PASS=...
DB_HOST=...
DB_NAME=...
DB_PORT=...
DB_SSL=true